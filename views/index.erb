<a href="/memos/new" class="btn btn-primary">新規作成</a>
<ul>
<% @memos.each_with_index do |memo, index| %>
  <li>
    <a href="/memos/<%= index %>">
      <%= memo[:title] %>
    </a>
  </li>
<% end %>
</ul>
